{
  "kind": {
    "Isotonic": {
      "xs": [0.01, 0.99],
      "ys": [0.01, 0.99]
    }
  },
  "n_fit": 0,
  "clamp_lo": 0.01,
  "clamp_hi": 0.99,
  "note": "Identity placeholder (2026-07): the previous artifact was fit on leaky data and amplified raw >=0.60 to exactly 1.0. Identity = no adjustment until a refit on real resolved forecasts ships via the runtime override (~/.openclaw/kalshi-monster/calibrator.json). n_fit=0 keeps the UI status honest ('raw model probability'). Unknown JSON fields are ignored by serde."
}
